#include<stdio.h>
#include<string.h>
int main()
{
char str[20]={"apple"};
int n,i;
n=strlen(str);
for(i=0;i<n;i++)
{
if(str[i]!='a'&&str[i]!='e')
{
    else if(str[i]!='i')&&(str[i]!='o')
    {
        else if(str[i]!='u')
{
printf("%s",str[i]);
}
}
}
return 0;
}
