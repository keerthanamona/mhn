#include<stdio.h>
#include<math.h>
int main(void)
{
	int n,i,b,e,c;
	printf("enter the number:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		b=pow(2,i);
		if(a>n)
		{
			e=i;
			break;
		}
	}
	c=pow(2,e);
	printf("\n%d",c);
	
	return 0;
}
